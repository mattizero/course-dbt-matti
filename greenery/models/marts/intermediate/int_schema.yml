version: 2

models:
  
  - name: int_events_pivoted_to_product
    description: Intermediate table pivoting and aggregating event data into product grain

  - name: int_events_pivoted_to_sessions
    description: Intermediate table pivoting and aggregating event data into session grain